import * as React from "react"
import MirCardContent from '@components/atoms/surfaces/MirCardContent';
import MirPaper from '@components/atoms/surfaces/MirPaper';
import { styled } from '@mui/material';

interface MirButtonProps {
  title: string;
  onClick?: () => void;
}

const MirCard = ({}) => {
  return (
    <>

    </>
  );
};

export default MirCard;
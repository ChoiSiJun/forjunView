import * as React from "react"
import CardContent from '@mui/material/CardContent';
import { styled } from '@mui/material';

const CardContentStyled = styled(CardContent)({});

const MirCardContent = ({}) => {
  return (
    <>
      <CardContent>
      </CardContent>
    </>
  );
}

export default MirCardContent;